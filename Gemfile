source 'https://rubygems.org'

# Matches GitHub Pages' build environment.
# Extra plugin gems listed separately can pin an ancient github-pages
# (Jekyll 3.9 / Liquid 4.0.3), which crash on Ruby 3.2+ (`tainted?`).
gem 'github-pages', group: :jekyll_plugins
gem 'webrick'
